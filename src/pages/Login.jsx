import { useState } from 'react';

export default function Login() {
    // login consts

    // handle submit function


    return (
        <div>
            <form>
                <button>Login</button>
            </form>
        </div>
    );
}